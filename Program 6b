
import matplotlib.pyplot as plt import numpy as np
def line_formatting_plot(): # Generate sample data x_values = np.linspace (0, 10, y_values1 = 2 * x_values + 3 y_values2 = 3 * x_values - 1
if
100) # 100 points from 0 to 10 # Linear equation: y = 2x + 3 # Another linear equation: y = 3x 1
# Create a plot with line formatting plt.plot(x_values, y_values1, label='Line 1 (y = 2x + 3)', color='blue', linestyle='- plt.plot(x_values, y_values2, label='Line 2 (y 3x 1)', color='red', linestyle='-
# Add labels and title
plt.xlabel('X-axis')
plt.ylabel('Y-axis')
plt.title('Linear Plot with Line Formatting')
# Add a legend
plt.legend()
# Show the plot
plt.show()
name =="main":
# Call the function to create the linear plot with line formatting
line_formatting_plot()
marker='0', markersize=5) marker's', markersize=5)